#!/usr/bin/node
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);

// On accède à la propriété 'value' de l'objet pour la modifier
myObject.value = 89;

console.log(myObject);
